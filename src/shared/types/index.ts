/**
 * Shared Types Index
 *
 * Export all shared TypeScript types and interfaces that are used across multiple microservices.
 * These are common data structures and type definitions.
 */

// Re-export types from core-ui via the main index to avoid module resolution issues
// Types are available through the main shared index and microservice-specific imports