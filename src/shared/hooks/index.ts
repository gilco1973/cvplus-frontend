/**
 * Shared Hooks Index
 *
 * Export all shared React hooks that are used across multiple microservices.
 * These are utility hooks for common functionality like data fetching, state management, etc.
 */

// Re-export hooks from core-ui via the main index to avoid module resolution issues
// Hooks are available through the main shared index